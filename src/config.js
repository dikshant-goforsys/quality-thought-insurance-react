// ⚙️ Update these when you connect real APIs.
// Leave API_BASE = "" (or "mock") to use mock data.
export const API_BASE = ""; // e.g., "https://api.yourdomain.com"
export const LIST_PATH = "/chat/messages?conversationId={conversationId}";
export const POST_PATH = "/chat/messages";
export const AUTH_TOKEN = ""; // e.g., "Bearer eyJhbGciOi..."
export const POLL_MS = 15000;
